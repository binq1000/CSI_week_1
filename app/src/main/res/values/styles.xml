<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="wrapHeightAndWidth">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@drawable/buttonshape</item>
        <item name="android:textColor">@color/button_text</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="buttonLeftStyle" parent="wrapHeightAndWidth">
        <item name="android:layout_alignParentLeft">true</item>
    </style>

    <style name="buttonRightStyle" parent="wrapHeightAndWidth">
        <item name="android:layout_alignParentRight">true</item>
    </style>

    <style name="buttonCenterStyle" parent="wrapHeightAndWidth">
        <item name="android:layout_centerInParent">true</item>
    </style>

    <style name="buttonCenterStyleLinear" parent="wrapHeightAndWidth">
        <item name="android:layout_gravity">center</item>
    </style>

    <style name="greenTableRowTextview">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">0px</item>
        <item name="android:layout_weight">5</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/greenishh</item>
    </style>

    <style name="contentHeader">
        <item name="android:layout_width">match_parent</item>
        <item name="android:paddingBottom">@dimen/activity_vertical_margin</item>
        <item name="android:paddingLeft">@dimen/activity_horizontal_margin</item>
        <item name="android:paddingRight">@dimen/activity_horizontal_margin</item>
        <item name="android:paddingTop">@dimen/activity_vertical_margin</item>
        <item name="android:orientation">vertical</item>
        <item name="android:background">@color/black</item>
    </style>

</resources>
